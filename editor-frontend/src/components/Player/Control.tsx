type ControlProps = {

};

const Control = ({ }: ControlProps) => {
    return (
        <div className="flex">
            <button>Player Controls</button>
        </div>
    );
};

export default Control;
